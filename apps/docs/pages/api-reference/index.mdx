# API Reference

Complete reference for all hooks, utilities, components, context, and types exported by the `react-formsteps-*` packages.

## Packages

| Package | Contents |
|---------|----------|
| `react-formsteps-core` | [useSteps](./api-reference/use-steps), [useStepForm](./api-reference/use-step-form), [Utilities](./api-reference/utilities), [Context](./api-reference/context), [Types](./api-reference/types) |
| `react-formsteps-ui` | [UI Components](./api-reference/components) |

## Quick import reference

```ts
// Core hooks
import { useSteps, useStepForm } from 'react-formsteps-core';

// Context
import { useStepsContext, StepsProvider } from 'react-formsteps-core';

// Utilities
import { validateStep, mergeSchemas, validateAllSteps } from 'react-formsteps-core';

// UI Components
import { Steps, Step, StepBar, StepNav } from 'react-formsteps-ui';
```
